<img src="<%= current_user.image %>" style="width: 100px; height: 100px; object-fit: cover; border-radius: 50%;">
<br>
@<%= current_user.username %>
<%= current_user.name %>

<%= link_to "退会", users_mypage_path(current_user.id), class: "destroy-user", method: :delete, "data-confirm" => "本当に退会しますか？" %>
